<h1>Create New Category</h1>

<%= form_for([:admin, @category]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit "Create Category" %>
<% end %>

<%= link_to 'Back to Categories List', admin_categories_path %>
